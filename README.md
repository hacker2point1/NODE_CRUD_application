This is a CRUD application using Node.js.
-----------------------------------------
To start it in the local server, choose the "dev" part and write the command in the terminal--
        --- npm run dev ,
For the production level start use the command--
        ---npm start
